name: Barchart tests

on:
  push:
    paths:
      - "Barchart/**"
      - ".github/workflows/barchart.yml"
  pull_request:
    paths:
      - "Barchart/**"
      - ".github/workflows/barchart.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: Barchart/requirements.txt
      - name: Install dependencies
        run: python -m pip install --upgrade pip && python -m pip install -r Barchart/requirements.txt
      - name: Compile package
        run: python -m compileall -q Barchart
      - name: Run tests
        run: python -m unittest discover -s Barchart/tests -v
